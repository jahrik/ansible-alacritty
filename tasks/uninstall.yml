---
- name: Remove alacritty binary, desktop entry, and icon (SteamOS)
  become: false
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - ~/.local/bin/alacritty
    - ~/.local/share/applications/Alacritty.desktop
    - ~/.local/share/icons/Alacritty.svg
  when: ansible_distribution == 'SteamOS'

- name: Uninstall alacritty (macOS)
  become: false
  community.general.homebrew_cask:
    name: alacritty
    state: absent
  when: ansible_os_family == 'Darwin'

- name: Uninstall alacritty
  become: true
  ansible.builtin.package:
    name:
      - alacritty
    state: absent
  when: ansible_os_family not in ['Darwin'] and ansible_distribution != 'SteamOS'

- name: Destroy ~/.config/alacritty directory
  become: false
  ansible.builtin.file:
    path: ~/.config/alacritty
    state: absent
